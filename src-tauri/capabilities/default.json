{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main radar window. Custom radar commands do not require permission entries.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
